<div>
	<%= form_for @profile, url: {action: "edit_do"} do |form| %>
		<%= f.label "First Name" %>
		<%= f.text_field :first_name %>
		<%= f.label "Last Name" %>
		<%= f.text_field :last_name %>
		<%= f.label "Birthday" %>
		<%= f.date_field :birthday %>
		<%= f.submit "Submit" %>
	<% end %>	
